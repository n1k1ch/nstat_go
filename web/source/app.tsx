import * as React from "react";
import * as ReactDOM from "react-dom";

import {Entries} from "./components/Entries";
import EntrySchema from "./components/EntrySchema";

ReactDOM.render(
    <Entries />,
    document.getElementById("app")
);